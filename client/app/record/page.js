import RecordPage from "./RecordPage";

export const metadata = {
  title: "Live transcribe",
  description: "Record audio with live transcription, stream to server",
};

export default function Page() {
  return <RecordPage />;
}
